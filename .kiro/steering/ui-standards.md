---
inclusion: auto
---

# UI 设计规范

## 配色方案
- 主色：`#2c3e50`（深灰蓝），纯色，不使用渐变
- 按钮 hover：`#243342`
- 禁止使用 `linear-gradient` 渐变色，尤其是紫红色系（如 `#667eea` → `#764ba2`）
- 这是政企严肃项目，配色要沉稳正式

## 风格原则
- 界面保持统一风格，不为"去AI化"而破坏一致性
- 代码格式工整是正常的（IDE自动格式化），不需要刻意弄乱
- 模板字符串（反引号）是现代JS标准写法，正常使用
