# Flask 全栈项目快速开发提示词

## 使用方法
将下面的提示词复制到AI对话中，把 `{项目名称}`、`{项目描述}` 替换成你的实际内容即可。
适用于：软著Demo、产品原型、个人项目、课程作业等任何需要快速出成果的Web项目。

---

## 提示词正文

```
你是一个专业的全栈开发工程师，现在需要快速开发一个Web项目：{项目名称}。

项目描述：{项目描述}

请严格遵循以下规范开发：

## 一、技术栈要求

后端：Python + Flask
- 原因：代码量适中、结构清晰、单文件即可运行、无需编译
- 数据库用 SQLite，不需要额外安装数据库服务
- 所有数据通过数据库存取，禁止在代码中写死任何业务数据

前端：原生 HTML5 + CSS3 + JavaScript
- 不使用 React/Vue 等框架，避免构建工具依赖
- 不使用 TypeScript，直接用 JS
- CSS 图标库用 Font Awesome（CDN引入）
- 不引入其他第三方JS库

## 二、数据规范（最重要）

### 绝对禁止写死的内容：
- 统计数字（总数、计数、百分比、金额）
- 表格中的具体数据行
- 下拉选项中的具体业务数据（如资产列表、人员列表）
- 表单中的默认值（如 value="张三"、value="13800138000"）
- 用户名密码的明文比对（如 if username === 'admin'）
- 具体的品牌、型号、人名、电话、邮箱等

### 正确做法：
- 统计数字：后端从数据库 COUNT/SUM 查询，通过模板变量 {{ variable }} 渲染
- 表格数据：后端查询后通过 {% for item in items %} 循环渲染
- 下拉选项：通过 API 接口动态加载，或后端传入模板变量循环生成
- 表单默认值：留空，或通过 JS 从 API 获取后填充
- 登录验证：通过后端 API 接口校验，前端 fetch 调用
- 示例数据：只在 database.py 的 insert_sample_data() 中集中管理

### 示例数据集中管理原则：
- 所有示例数据只出现在 database.py 的初始化函数中
- 前端模板和 JS 文件中不出现任何具体业务数据
- 后端路由中不出现硬编码的统计数字

## 三、界面设计规范

### 整体布局：
- 左侧固定导航栏（渐变色背景），右侧内容区
- 导航栏顶部显示系统全称 + 图标
- 导航栏包含用户信息和退出按钮
- 内容区顶部为统计卡片，下方为数据表格

### 登录页面：
- 居中卡片式布局，渐变背景
- 显示系统全称和 Logo 图标
- 不显示默认账号密码提示
- 不显示版权信息

### 统计卡片：
- 使用 CSS Grid 自适应布局
- 每个卡片包含图标、数字、标签
- 数字全部从后端传入，禁止写死

### 数据表格：
- 统一的表头样式，字体大小一致
- 禁止对某些列单独加粗或使用特殊字体
- 操作列的按钮要有实际功能，禁止出现"开发中"提示
- 所有按钮点击后都要有响应（弹窗、提示、跳转等）

### 弹窗/模态框：
- 统一的弹窗样式（header + body + footer）
- 表单弹窗的字段值从 API 获取填充，不写死
- 关闭按钮和背景点击都能关闭

### 配色方案：
- 主色调：#667eea（蓝紫渐变）
- 成功：#28a745
- 警告：#ffc107
- 危险：#dc3545
- 信息：#17a2b8

## 四、代码结构

```
project_name/
├── app.py              # Flask主应用，所有路由
├── database.py         # 数据库初始化 + 示例数据（唯一允许写具体数据的地方）
├── static/
│   ├── css/style.css   # 全局样式
│   └── js/main.js      # 全局交互逻辑
├── templates/
│   ├── base.html       # 基础模板（导航栏）
│   ├── login.html      # 登录页
│   ├── index.html      # 路由分发模板
│   └── modules/        # 各功能模块模板
│       ├── module_a.html
│       ├── module_b.html
│       └── ...
└── data/
    └── app.db          # SQLite数据库文件（运行时生成）
```

## 五、功能模块通用模式

每个功能模块都遵循相同模式：

### 后端路由（app.py）：
- 从数据库查询数据
- 计算统计数字
- 传入模板渲染

### 模板文件（modules/xxx.html）：
- 统计卡片区：{{ 变量 }} 渲染
- 数据表格区：{% for %} 循环渲染
- 操作按钮：调用 JS 函数

### JS交互（main.js）：
- 表单弹窗：动态生成 HTML
- 数据提交：fetch API 调用后端接口
- 下拉选项：从 /api/xxx 接口动态加载
- 操作反馈：showMessage() 统一提示

## 六、API接口规范

- GET /api/资源 → 获取列表
- POST /api/资源 → 新增
- GET /api/资源/<id> → 获取详情
- PUT /api/资源/<id> → 更新
- DELETE /api/资源/<id> → 删除
- 统一返回格式：{"success": true/false, "message": "...", "data": ...}

## 七、GitHub README 规范

项目需要一个专业的 README.md，包含以下内容：

### 顶部 Shields.io 徽章（必须）：
在 README 最顶部添加彩色徽章，格式为图片链接：
```
![Python](https://img.shields.io/badge/Python-3.10-blue)
![Flask](https://img.shields.io/badge/Flask-2.0+-green)
![SQLite](https://img.shields.io/badge/Database-SQLite-orange)
![License](https://img.shields.io/badge/License-MIT-yellow)
```
根据项目实际用到的技术和特色，额外添加相关徽章。

### README 结构：
1. 徽章行
2. 项目名称 + 一句话介绍
3. 系统截图（如有）
4. 功能特性列表
5. 技术栈说明
6. 快速开始（安装 + 运行命令）
7. 项目结构树
8. API 接口说明
9. License

### .gitignore 文件（必须）：
```
data/
__pycache__/
*.pyc
.env
```

### License 文件：
默认使用 MIT License，创建 LICENSE 文件。

## 八、软著代码审查清单

开发完成后，逐项检查：
- [ ] 所有统计数字都来自数据库查询，不是写死的
- [ ] 所有表格数据都通过 for 循环渲染，不是写死的 HTML 行
- [ ] 所有下拉选项都是动态加载或模板循环，不是写死的 option
- [ ] 表单默认值为空或从 API 获取，不是写死的 value
- [ ] 登录验证通过后端 API，不是前端明文比对
- [ ] 没有出现具体的人名、电话、邮箱等个人信息
- [ ] 没有出现具体的品牌、型号等商业信息
- [ ] 所有按钮都有实际功能，没有"开发中"提示
- [ ] 示例数据只在 database.py 中集中管理
- [ ] 界面字体风格统一，没有某列单独加粗或特殊字体

现在请开始开发，先给出完整的项目结构，然后逐个文件实现。
```
